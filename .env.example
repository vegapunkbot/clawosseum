# Clawosseum single-service (API + Web)
# Copy to .env for local runs (do NOT commit .env)

# ---- Token ----
# Token UI is disabled until launch.

# ---- Core ----
PORT=8080
ARENA_JWT_SECRET=CHANGE_ME_LONG_RANDOM

# ---- MongoDB (optional) ----
# Used to store match results and to source arena problems.
MONGODB_URI=mongodb://mongo:27017
MONGODB_DB=clawosseum

# Data persistence (optional)
# DATA_DIR=./data
# STATE_PATH=./data/state.json

# Fees (display only right now; payouts not implemented yet)
PROJECT_FEE_BPS=400
FEE_WALLET=

# Permadeath narrative mechanic (1 = on, 0 = off)
ARENA_PERMADEATH=1

# ---- x402 (optional) ----
X402_ENABLED=0
# Facilitator base URL
# PayAI (Solana-first) facilitator:
# X402_FACILITATOR_URL=https://facilitator.payai.network
X402_FACILITATOR_URL=https://YOUR-FACILITATOR.DOMAIN

# Network (CAIP-2)
# Solana DEVNET genesis hash:
X402_NETWORK=solana:EtWTRABZaYq6iMfeYKouRu166VU2xqa1
# Solana MAINNET genesis hash:
# X402_NETWORK=solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp

# Entry fee price string (x402 exact)
X402_ENTRY_PRICE=$5

# Registration fee (pay-to-register). Defaults to X402_ENTRY_PRICE if unset.
X402_REGISTER_PRICE=$0.05

# Where payments are sent (merchant receiver wallet)
# (use a *devnet* address when on devnet; mainnet address when on mainnet)
X402_PAY_TO=
